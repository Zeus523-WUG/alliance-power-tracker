* { box-sizing: border-box; }
:root { --bg:#0f172a; --panel:#111827; --text:#e5e7eb; --muted:#9ca3af; --primary:#4f46e5; --border:rgba(148,163,184,.2); }
body { margin:0; font-family:Arial,sans-serif; background:linear-gradient(135deg,#0f172a,#111827); color:var(--text); }
.auth-body,.dashboard-body { min-height:100vh; display:flex; align-items:center; justify-content:center; padding:24px; }
.auth-card { width:min(100%,460px); background:rgba(17,24,39,.96); border:1px solid var(--border); border-radius:18px; padding:28px; box-shadow:0 16px 40px rgba(0,0,0,.25); }
h1,h2 { margin-top:0; }
form { display:flex; flex-direction:column; gap:14px; }
label { font-size:.95rem; font-weight:600; }
input,select,button { width:100%; padding:12px 14px; border-radius:10px; border:1px solid var(--border); font-size:1rem; }
input,select { background:rgba(15,23,42,.7); color:var(--text); }
button { background:var(--primary); color:#fff; border:0; font-weight:700; cursor:pointer; }
button:hover { background:#4338ca; }
.alert { padding:12px 14px; border-radius:10px; margin-bottom:18px; font-weight:600; }
.alert.error { background:rgba(239,68,68,.12); border:1px solid rgba(239,68,68,.35); color:#fecaca; }
.link-row { margin-top:18px; text-align:center; } a { color:var(--text); }
.container { width:min(1200px,100%); margin:30px auto; padding:0 20px 36px; }
.topbar { display:flex; justify-content:space-between; align-items:center; margin-bottom:20px; }
.topbar-actions { display:flex; align-items:center; gap:12px; }
.date-pill,.logout { padding:8px 12px; border-radius:999px; font-size:.8rem; }
.date-pill { background:rgba(79,70,229,.18); border:1px solid rgba(99,102,241,.5); }
.logout { text-decoration:none; background:rgba(239,68,68,.12); border:1px solid rgba(239,68,68,.25); }
.panel { background:rgba(17,24,39,.95); border:1px solid var(--border); border-radius:18px; padding:24px; margin-bottom:24px; }
.field-group { display:flex; flex-direction:column; gap:8px; }
.checkbox-row { display:flex; align-items:center; gap:10px; }
.checkbox-row input { width:auto; }
.table-wrap { overflow-x:auto; } table { width:100%; border-collapse:collapse; } th,td { padding:12px 10px; border-bottom:1px solid var(--border); text-align:left; } th { color:var(--muted); font-size:.8rem; text-transform:uppercase; }
.chart-panel { min-height:360px; } #historyChart { width:100%; height:300px; }
@media(max-width:768px) { .topbar { flex-direction:column; align-items:flex-start; gap:12px; } .topbar-actions { width:100%; justify-content:space-between; } }
